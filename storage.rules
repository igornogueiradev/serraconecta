rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Logos de agência: leitura pública, escrita apenas pelo próprio usuário
    match /logos/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
